@{
    ViewData["Title"] = "Index";
}
@model List<Products>

<table class="table table-striped table-light">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">First</th>
      <th scope="col">Last</th>
      <th scope="col">Ages</th>
      <th scope="col">Length</th>
      <th scope="col">Wage</th>
      <th scope="col">Handle</th>
    </tr>
  </thead>
  <tbody>
       @foreach (var item in Model)
       {

        <tr>
            <td>@item.Id</td>
            <td>@item.FirstName</td>
            <td>@item.LastName</td>
            <td>@item.Age</td>
            <td>@item.length</td>
            <td>@item.wage</td>
            <td><a class="btn btn-success" asp-controller="products" asp-action="update" asp-route-id="@item.Id">Update</a>
                            <a asp-controller="products" asp-action="remove" asp-route-id="@item.Id"
                                class="btn btn-danger">Close</a></td>
        </tr>
       }
       
      </tbody>
</table>
<!--<a href="Products/Add"> <button class="btn btn-primary">ürün ekle</button></a>-->
